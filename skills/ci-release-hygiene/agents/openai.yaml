interface:
  display_name: "CI Release Hygiene"
  short_description: "Tie CI and release evidence to exact commits"
  default_prompt: "Use $ci-release-hygiene to verify that CI and release evidence matches the current commit."
